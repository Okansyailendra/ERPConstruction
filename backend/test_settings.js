require('dotenv').config(); const db = require('./db'); async function run() { try { await db.query('CREATE TABLE company_settings (id INT AUTO_INCREMENT PRIMARY KEY, company_name VARCHAR(255), npwp VARCHAR(255), address TEXT, email VARCHAR(255), phone VARCHAR(50), logo_url VARCHAR(255))'); await db.query('INSERT INTO company_settings (company_name, npwp, address, email, phone) VALUES ("PT. ConstructERP Indonesia", "01.234.567.8-901.000", "Gedung Sudirman Lantai 12", "info@constructerp.id", "0215550123")'); console.log('Created'); } catch(e) { console.log(e); } process.exit(0); } run();
